# STM32F429 Boot 详细设计文档

## 1. 文档信息

- **文档名称**：STM32F429 Boot 详细设计文档
- **适用对象**：基于 STM32F429 的双分区启动与 U 盘升级系统
- **文档目标**：定义 Boot 的功能边界、存储布局、启动策略、升级流程、异常恢复策略、配置管理、日志管理及工程组织规范
- **输出形式**：Markdown 设计文档
- **说明**：本文档不包含源码实现，仅描述完整设计方案

---

## 2. 设计目标

本 Boot 方案面向 STM32F429 平台，目标如下：

1. 建立独立 Boot 工程，并使用自建 CMake 进行构建管理。
2. 基于内部 Flash 实现 Boot + 双 App + 配置区 的分区布局。
3. 上电后优先检测 U 盘，在限定时间内判断是否进入升级流程。
4. 在无 U 盘或 U 盘不合法时，自动选择可启动的 App 分区运行。
5. 支持从 U 盘读取升级包与配置文件，完成：
   - App 镜像写入
   - 配置区内容写入
   - 外置 Flash 数据写入与导出
6. 支持升级结果记录，并将操作日志写入 U 盘。
7. 支持升级失败保护、启动失败回退和恢复模式，避免设备变砖。
8. 提高后续维护性、调试性与可扩展性。

---

## 3. 设计原则

### 3.1 可靠性优先

Boot 是整机可恢复能力的核心，应优先保证：

- 不覆盖当前唯一可运行镜像
- 中断升级不影响已确认稳定版本
- 启动失败可自动回滚
- 关键状态可持久化保存

### 3.2 状态可追踪

所有关键动作应可记录：

- 启动来源
- U 盘识别结果
- 升级操作过程
- 写入结果
- 错误原因
- 最终启动决策

### 3.3 分层解耦

Boot 内部设计应按职责解耦，避免将 USB、文件系统、Flash 管理、镜像校验、状态管理等逻辑混杂在一起。

### 3.4 可扩展性

后续应可方便支持：

- 更多升级对象
- 更多存储介质
- 更强安全机制
- 更复杂的升级规则
- 外部工具链配套

---

## 4. 系统总体架构

Boot 系统建议划分为以下几个功能子系统：

1. **启动控制模块**
   - 负责上电流程、模式切换、启动决策、跳转应用
2. **镜像管理模块**
   - 负责 App 合法性检查、镜像头校验、镜像版本管理
3. **U 盘升级模块**
   - 负责 U 盘识别、升级盘合法性判断、配置文件解析、升级动作执行
4. **Flash 管理模块**
   - 负责内部 Flash 擦写、配置区写入、元数据保存
5. **外置 Flash 管理模块**
   - 负责外置存储擦除、写入、校验、数据导出
6. **日志管理模块**
   - 负责升级与启动过程日志输出与摘要保存
7. **异常恢复模块**
   - 负责回滚、恢复模式和故障场景处理

---

## 5. 内部 Flash 布局设计

### 5.1 固定分区布局

内部 Flash 按如下方式划分：

| 分区 | 大小 | 地址范围 |
|------|------:|-----------|
| Boot | 128KB | `0x0800_0000 - 0x0801_FFFF` |
| App1 | 896KB | `0x0802_0000 - 0x080F_FFFF` |
| Config | 128KB | `0x0810_0000 - 0x0811_FFFF` |
| App2 | 896KB | `0x0812_0000 - 0x081F_FFFF` |

### 5.2 分区职责说明

#### Boot 分区

用于存放 Boot 固件本体，仅负责：

- 初始化必要硬件
- 检测升级介质
- 管理启动流程
- 校验镜像合法性
- 执行升级动作
- 记录启动和升级状态
- 跳转 App

Boot 分区不得存放业务功能代码。

#### App1 / App2 分区

用于存放两个独立应用镜像，实现：

- A/B 双分区启动
- 升级写入到非活动槽
- 启动失败自动回退

#### Config 分区

用于存放以下内容：

- Boot 控制块
- 设备运行配置
- 升级结果摘要
- 预留扩展数据

---

## 6. Config 分区内部组织设计

建议对 128KB Config 区进行逻辑子分区划分，以降低耦合并提升可靠性。

| 子区域 | 建议大小 | 说明 |
|--------|---------:|------|
| BootCtrl_A | 8KB | Boot 控制块副本 A |
| BootCtrl_B | 8KB | Boot 控制块副本 B |
| SysConfig | 16KB | 设备业务配置 |
| ConfigBackup | 16KB | 配置备份区 |
| BootSummaryLog | 32KB | 最近启动/升级摘要 |
| Reserved | 48KB | 后续扩展 |

### 6.1 设计目的

- 双备份保存 Boot 控制信息，避免断电写坏
- 配置区与控制区隔离，防止相互影响
- 内部保留摘要日志，即使 U 盘日志写失败也可离线分析

---

## 7. Boot 控制块设计

Boot 控制块是整个 Boot 系统状态机的核心。

### 7.1 Boot 控制块职责

用于记录：

- 当前活动槽
- 已确认稳定槽
- 待验证启动槽
- 启动尝试次数
- 各槽镜像合法性与版本信息
- 升级是否进行中
- 是否需要回滚
- 最近一次错误码
- 启动策略参数

### 7.2 Boot 控制块建议内容

Boot 控制块应至少包含以下信息：

- 魔数
- 结构版本号
- 数据序列号
- 当前活动槽编号
- 已确认槽编号
- 待验证槽编号
- 启动尝试计数
- 最大尝试次数
- App1 镜像信息
  - 版本
  - 大小
  - CRC
  - 有效标记
- App2 镜像信息
  - 版本
  - 大小
  - CRC
  - 有效标记
- 升级中标记
- 回滚标记
- 最近错误码
- 保留字段
- 整体 CRC

### 7.3 双备份写入机制

Boot 控制块建议使用 A/B 双副本。

#### 写入原则

1. 新数据生成后先计算完整校验。
2. 写入备用副本。
3. 写入成功并读回校验通过后，递增序列号。
4. 启动时选择序列号最大且校验正确的副本。

#### 设计收益

- 避免写入中断导致控制区全损
- 启动时可自动恢复到最近一次有效状态

---

## 8. App 镜像格式设计

为提高 Boot 对镜像的判断准确性，不建议使用裸二进制直接刷入。建议所有 App 镜像带统一镜像头。

### 8.1 镜像头作用

镜像头用于描述：

- 镜像身份
- 目标槽位
- 镜像长度
- 入口信息
- 版本信息
- 校验信息
- 产品型号信息
- 板型信息

### 8.2 镜像头建议字段

镜像头应至少包含：

- 固定魔数
- 头版本
- 镜像类型
- 目标槽
- 镜像长度
- 加载地址
- 入口地址
- 版本号
- CRC32
- 标志位
- 产品名
- 板型名
- 构建时间
- 保留字段

### 8.3 镜像布局建议

建议一个 App 分区内布局如下：

1. 分区起始处为镜像头。
2. 镜像头后为中断向量表。
3. 向量表后为应用正文。

### 8.4 设计收益

- Boot 能准确识别镜像是否属于当前设备
- 支持版本管理
- 支持镜像类型扩展
- 可避免单纯依赖向量表判断造成误判

---

## 9. App 合法性判定规则

Boot 在决定是否可启动某个 App 时，应按固定规则进行校验。

### 9.1 基础校验项

1. 镜像头魔数正确。
2. 镜像头版本受支持。
3. 目标槽与分区位置一致。
4. 镜像大小不超过分区边界。
5. 加载地址合法。
6. 入口地址位于该分区合法范围。
7. 产品信息与设备匹配。
8. 板型信息与设备匹配。
9. 镜像 CRC32 校验通过。

### 9.2 向量表校验项

1. 初始栈指针位于 SRAM 合法范围。
2. Reset Handler 位于该 App 分区范围。
3. 向量表基址与镜像头中的入口关系合理。

### 9.3 合法性结论

只有当上述所有必要项均通过时，才将该槽标记为合法可启动。

---

## 10. 启动模式设计

系统启动模式建议包含以下几类：

### 10.1 正常启动模式

默认模式。上电后按规则判断是否进入升级或启动 App。

### 10.2 升级模式

检测到合法升级 U 盘后进入，用于执行：

- App 写入
- 配置写入
- 外置 Flash 写入
- 日志输出

### 10.3 恢复模式

当两个 App 均不可启动时进入。用于等待恢复介质或提供故障诊断能力。

### 10.4 强制升级模式

可通过硬件输入或特殊配置进入。进入后不立即跳转 App，而是持续等待升级介质。

---

## 11. 上电主流程设计

### 11.1 流程概述

上电后 Boot 应执行以下主流程：

1. 基础硬件初始化。
2. 读取 Boot 控制块。
3. 启动日志系统。
4. 启动 USB Host。
5. 在限定时间窗口内检测 U 盘。
6. 若检测到合法升级盘，则执行升级动作。
7. 升级结束后进入启动仲裁。
8. 若未检测到合法升级盘，则直接进入启动仲裁。
9. 若无合法 App，则进入恢复模式。

### 11.2 基础初始化内容

Boot 应只初始化必要资源：

- 时钟
- GPIO
- 基础中断
- 看门狗策略
- 串口调试接口（可选）
- CRC 外设（如使用）
- USB Host
- 内部 Flash 访问接口
- 外置 Flash 接口（如需要）

### 11.3 启动窗口设计

建议使用可配置的 U 盘检测窗口，默认值建议为 600ms。

#### 原因

- 部分 U 盘枚举较慢
- 500ms 容易形成边界误判
- Boot 不宜等待过长，避免影响正常开机体验

#### 建议

默认等待窗口为 600ms，并允许通过配置项修改。

---

## 12. U 盘检测与识别设计

### 12.1 检测目标

Boot 需要区分：

- 未插入 U 盘
- 插入普通 U 盘
- 插入合法升级 U 盘

### 12.2 基本检测过程

1. 启动 USB Host。
2. 在窗口时间内轮询设备接入状态。
3. 设备接入后挂载文件系统。
4. 判断是否存在专用升级目录和配置文件。

### 12.3 普通 U 盘与升级 U 盘的区分

建议采用多级规则识别升级盘。

#### 一级规则

必须存在固定路径：

- `/boot/manifest.json`

#### 二级规则

manifest 中应包含：

- 供应商标识
- 产品标识
- 板型标识

并与当前设备匹配。

#### 三级规则

可选存在专用标识文件，例如：

- `/boot/.boot_token`

#### 四级规则

可选支持数字签名，用于验证升级包来源合法性。

### 12.4 识别结论

只有同时满足目录结构与设备匹配规则时，才认定为合法升级 U 盘。

---

## 13. U 盘目录结构设计

建议升级 U 盘采用固定目录布局：

```text
/boot/
    manifest.json
    device.cfg
    app1.bin
    app2.bin
    config.bin
    extflash.bin
    signature.sig
    /log/
```

### 13.1 各文件作用

- `manifest.json`
  - 升级动作描述文件
- `device.cfg`
  - 设备级附加配置，可选
- `app1.bin`
  - 写入 App1 的镜像文件
- `app2.bin`
  - 写入 App2 的镜像文件
- `config.bin`
  - 配置区数据文件
- `extflash.bin`
  - 外置 Flash 数据文件
- `signature.sig`
  - 升级包签名文件，可选
- `log/`
  - Boot 输出日志目录

### 13.2 设计原则

- 所有升级动作由 manifest 显式描述
- Boot 不根据文件名猜测写入行为
- 同一目录结构便于产线、售后、测试统一使用

---

## 14. Manifest 设计

Manifest 是升级流程的调度核心。

### 14.1 Manifest 作用

用于定义：

- 升级包格式版本
- 适用产品型号
- 适用板型
- 升级包版本
- 目标设备限制
- 具体操作列表
- 每个文件的大小与校验信息
- 升级策略控制项

### 14.2 Manifest 应包含的信息

建议包括以下信息：

- 格式版本
- 厂商名
- 产品名
- 板型名
- Bundle 版本
- Bundle ID
- 设备序列号限制
- 操作列表

### 14.3 操作列表类型

建议支持以下操作类型：

- `write_app`
- `write_config`
- `write_extflash`
- `erase_extflash`
- `dump_extflash`
- `set_boot_flag`

### 14.4 设计要求

- 每个操作应明确目标对象
- 每个文件操作应明确文件名、目标位置、大小、校验值
- 不允许存在含义不明确的隐式动作

---

## 15. 升级操作设计

### 15.1 总体原则

升级执行时必须遵循以下原则：

1. 不破坏当前已确认可运行版本。
2. 不允许一半成功一半未知。
3. 每个动作完成后应校验。
4. 所有关键步骤应记录日志。
5. 升级状态应持久化，以支持异常恢复。

---

## 16. App 升级策略设计

### 16.1 写入目标选择原则

默认情况下，新版本应写入非当前已确认槽。

#### 示例

- 若当前确认运行槽为 App1，则新镜像默认写入 App2。
- 若当前确认运行槽为 App2，则新镜像默认写入 App1。

### 16.2 原因

- 保留一个已确认可启动版本
- 升级失败时可立即回滚
- 降低现场升级风险

### 16.3 App 写入流程

1. 读取 manifest 中的 App 操作项。
2. 检查目标槽是否合法。
3. 检查镜像头与目标槽匹配。
4. 校验文件大小与 CRC。
5. 擦除目标分区。
6. 分块写入镜像。
7. 写入结束后进行整镜像校验。
8. 更新 Boot 控制块中的目标槽镜像信息。
9. 设置待验证启动槽。
10. 日志记录升级结果。

### 16.4 升级提交策略

写入成功后，不应立即将新镜像设为“已确认版本”。

正确做法是：

- 设置为待验证槽
- 下次启动优先尝试该槽
- 由新 App 在启动完成后主动确认成功

---

## 17. App 启动确认与回滚设计

### 17.1 待验证槽概念

待验证槽表示：

- 镜像写入成功
- Boot 允许尝试启动
- 但尚未确认其稳定运行

### 17.2 启动尝试机制

Boot 应记录：

- 当前待验证槽
- 已尝试启动次数
- 最大允许尝试次数

### 17.3 新 App 确认机制

新 App 成功启动并完成初始化后，应主动向 Boot 控制区写入“启动确认成功”标记。

确认成功后：

- 待验证槽转为已确认槽
- 活动槽更新为该槽
- 启动尝试计数清零
- 清除回滚标记

### 17.4 启动失败判定

若待验证槽在若干次重启后仍未完成确认，则认为启动失败。

失败信号通常包括：

- 启动过程中死机
- 看门狗复位
- App 初始化异常复位
- 长时间未设置确认标志

### 17.5 回滚策略

当待验证槽超过最大尝试次数后：

1. 标记该槽本次升级失败。
2. 清除待验证状态。
3. 回滚至已确认槽。
4. 日志记录回滚原因。

---

## 18. 配置区写入设计

### 18.1 配置写入需求

Boot 应支持通过升级盘更新设备配置数据。

### 18.2 配置写入方式建议

建议优先支持整包写入，即：

- 从 `config.bin` 读取完整配置镜像
- 按规则写入 SysConfig 区

### 18.3 保护策略

配置更新时建议保留以下敏感内容不被覆盖：

- 设备唯一编号
- 出厂校准参数
- 安全密钥区
- 不允许被升级包修改的制造信息

### 18.4 写入流程

1. 读取配置文件。
2. 校验文件大小和 CRC。
3. 解析配置版本。
4. 读出当前配置。
5. 合并保留字段。
6. 擦除目标配置区。
7. 写入新配置。
8. 读回校验。
9. 写日志。

### 18.5 配置备份策略

建议在 ConfigBackup 区保留一份最近一次有效配置，以便配置升级失败时恢复。

---

## 19. 外置 Flash 操作设计

### 19.1 支持目标

Boot 应支持通过 U 盘执行外置 Flash 的如下操作：

- 擦除指定区域
- 将文件写入指定区域
- 从指定区域导出数据到 U 盘

### 19.2 适用场景

- 字库更新
- 资源文件更新
- 参数库存储
- 数据备份导出
- 故障分析数据提取

### 19.3 写入流程

1. 解析 manifest 中的外置 Flash 写入项。
2. 校验 offset 和 size 是否在合法范围。
3. 校验文件 CRC。
4. 擦除对应外置 Flash 扇区。
5. 分块写入。
6. 读回校验。
7. 写日志。

### 19.4 导出流程

1. 解析导出动作。
2. 从外置 Flash 读取指定区间。
3. 生成输出文件。
4. 保存到 U 盘指定路径。
5. 记录日志。

### 19.5 风险控制

- 不允许越界写入
- 不允许未声明区域被写
- 必须在 manifest 中显式描述所有外置 Flash 操作

---

## 20. 启动决策设计

### 20.1 启动决策目标

在升级和异常场景下，Boot 应保证：

- 优先尝试待验证镜像
- 启动失败可自动回退
- 正常情况下优先启动当前活动槽
- 至少存在一个合法镜像即可运行

### 20.2 启动决策优先级

建议按以下顺序判断：

1. 若存在待验证槽：
   - 检查其合法性
   - 若合法且尝试次数未超限，则优先启动
   - 若不合法或已超限，则回滚
2. 若无待验证槽：
   - 优先启动当前活动槽
3. 若当前活动槽不合法：
   - 尝试另一槽
4. 若两槽都不合法：
   - 进入恢复模式

### 20.3 当前活动槽概念

当前活动槽表示 Boot 默认的优先启动槽，不一定等同于已确认槽，但正常情况下两者通常一致。

---

## 21. 恢复模式设计

### 21.1 进入条件

满足任一条件时进入恢复模式：

- App1 和 App2 均不合法
- 升级中断后无可回滚版本
- 启动决策无法选择可运行槽

### 21.2 恢复模式行为

恢复模式建议包含以下能力：

- LED 状态指示
- 串口错误输出
- 持续轮询 U 盘
- 检测到合法升级盘后自动执行恢复升级

### 21.3 设计目标

恢复模式应尽量做到：

- 不死机
- 不无限复位
- 不丢失故障原因
- 可通过现场介质恢复

---

## 22. 强制升级模式设计

### 22.1 设计目的

在调试、产线、售后场景下，应支持不依赖自动时间窗口而进入升级模式。

### 22.2 进入方式建议

可支持如下方式之一或组合：

- 上电时检测某按键按下
- 检测某 GPIO 电平
- 检测配置区中的升级请求标志
- 检测 App 设置的软请求标志

### 22.3 行为定义

进入强制升级模式后：

- 不立即跳转 App
- 持续等待合法 U 盘
- 执行升级后再进入启动决策

---

## 23. 日志系统设计

### 23.1 日志目标

日志系统用于支持：

- 升级过程追踪
- 启动路径追踪
- 异常原因定位
- 售后问题分析

### 23.2 日志输出位置

建议同时保留两类日志：

1. U 盘详细日志
2. 内部摘要日志

### 23.3 U 盘详细日志

建议输出到：

```text
/boot/log/
```

文件名建议带时间戳或序号。

### 23.4 日志内容建议

应记录以下内容：

- Boot 版本
- 上电开始
- U 盘检测结果
- 升级盘合法性判断
- manifest 解析结果
- 每项升级操作开始与结束
- 进度信息
- 校验结果
- 启动决策结果
- 回滚与异常信息
- 最终结果

### 23.5 内部摘要日志

内部摘要日志建议保存：

- 最近一次启动结果
- 最近一次升级结果
- 最近一次错误码
- 最近一次回滚原因
- 目标槽信息

### 23.6 日志等级建议

建议支持以下等级：

- INFO
- WARN
- ERROR
- RESULT

---

## 24. 错误码设计

### 24.1 设计原则

- 错误码应按模块分段
- 每个错误码应唯一
- 错误码应可在日志中直接定位问题来源

### 24.2 建议错误码分区

#### U 盘与文件系统类

- U 盘未检测到
- 文件系统挂载失败
- 升级目录不存在
- manifest 不存在
- manifest 格式错误
- 文件缺失
- 文件大小不匹配
- 文件 CRC 错误

#### 设备匹配类

- 产品型号不匹配
- 板型不匹配
- 设备序列号不匹配
- 升级包版本不允许

#### 内部 Flash 类

- 扇区擦除失败
- 写入失败
- 读回校验失败
- 地址越界

#### App 镜像类

- 镜像头错误
- 镜像类型错误
- 目标槽不匹配
- 镜像大小越界
- 向量表非法
- 镜像 CRC 错误

#### 外置 Flash 类

- 擦除失败
- 写入失败
- 读回失败
- 导出失败
- 偏移越界

#### 启动控制类

- Boot 控制块损坏
- 无有效控制块
- 待验证槽超出尝试次数
- 回滚失败
- 无可启动镜像

---

## 25. 安全性设计

### 25.1 基础安全措施

至少建议实现以下校验：

- 文件 CRC32
- 产品型号匹配
- 板型匹配
- 镜像头合法性检查

### 25.2 版本策略

可支持以下策略之一：

- 允许任意版本升级
- 只允许高版本升级
- 默认禁止回退，除非 manifest 指定允许回退

### 25.3 增强安全措施

若对升级包来源有控制要求，建议增加：

- manifest 摘要计算
- 数字签名校验
- 内置公钥验签

### 25.4 设计建议

若当前目标以稳定性和交付效率为主，可先做基础版；若后续面向量产和现场升级，建议尽早预留签名校验接口。

---

## 26. 看门狗与异常复位策略

### 26.1 Boot 阶段看门狗策略

Boot 阶段是否启用看门狗，应视启动时间预算决定。

建议：

- 正常启动路径中保持 Boot 尽快完成
- 升级路径中若耗时较长，应周期性喂狗
- 进入恢复模式时应有明确的喂狗策略或控制策略

### 26.2 与待验证启动结合

若待验证镜像启动后因异常导致看门狗复位，Boot 下次启动时应：

- 识别为一次失败尝试
- 累加待验证槽尝试次数
- 达到上限后回滚

---

## 27. 断电与中断恢复设计

### 27.1 升级中断风险

升级过程中可能出现：

- 断电
- U 盘拔出
- 写入失败
- 外设异常

### 27.2 防护原则

1. 不覆盖当前确认版本。
2. 所有关键状态在操作前后更新。
3. 使用双备份控制块。
4. 写完后必须重新校验。
5. 任何未完成的升级不应自动视为成功。

### 27.3 中断后恢复逻辑

若 Boot 检测到上次升级未完成：

- 检查当前确认槽是否仍有效
- 若有效则优先回退运行确认槽
- 若无确认槽但另一槽有效，则运行另一槽
- 若都无效，则进入恢复模式

---

## 28. 跳转 App 设计要求

### 28.1 跳转前清理要求

Boot 在跳转前应完成以下清理：

- 关闭中断
- 关闭 SysTick
- 清除中断挂起
- 恢复中断控制器状态
- 设置向量表偏移
- 设置主栈指针

### 28.2 跳转对象要求

跳转目标必须是经过合法性判定的镜像入口。

### 28.3 注意事项

- 若镜像带头部，向量表地址不一定等于分区起始地址
- App 工程链接地址必须与镜像格式一致
- Boot 与 App 对向量表位置的理解必须统一

---

## 29. CMake 工程组织设计

### 29.1 设计目标

建立 Boot 独立工程，具备：

- 可复用
- 可维护
- 可跨环境构建
- 与 App 工程解耦

### 29.2 目录结构建议

建议工程至少分为以下逻辑目录：

- 根构建文件
- 工具链配置
- 链接脚本
- 核心启动文件
- 驱动层
- 中间件层
- Boot 业务逻辑层
- 配置文件目录
- 打包脚本目录

### 29.3 模块划分建议

Boot 工程内部建议拆分为：

- 主流程模块
- 启动控制模块
- 镜像校验模块
- U 盘访问模块
- 文件解析模块
- 内部 Flash 管理模块
- 外置 Flash 管理模块
- 日志模块
- 错误码模块
- 配置模块

### 29.4 链接脚本要求

应分别准备：

- Boot 链接脚本
- App1 链接脚本
- App2 链接脚本

保证：

- Boot 固定链接到 Boot 分区
- App1 固定链接到 App1 分区
- App2 固定链接到 App2 分区
- 如使用镜像头，应用向量表偏移需与布局一致

---

## 30. Boot 与 App 的接口设计

### 30.1 必要接口

虽然本文不提供代码，但设计上 Boot 与 App 之间至少需要一组约定接口。

#### App 启动确认接口

App 成功启动后，应能向 Config 区写入“确认成功”。

#### App 请求升级接口

App 可通过设置某个标志位请求下次进入强制升级模式。

#### App 获取启动原因接口

可支持 App 读取：

- 当前运行槽
- 是否由升级后首次启动
- 是否为回滚后启动
- 最近错误码

### 30.2 设计收益

- 提升 App 与 Boot 协同能力
- 有助于实现升级后自检
- 有助于故障分析和业务策略控制

---

## 31. 启动状态机设计

### 31.1 状态机建议

Boot 整体可抽象为如下状态：

1. 初始化状态
2. 读取控制块状态
3. USB 检测状态
4. U 盘合法性校验状态
5. Manifest 解析状态
6. 升级执行状态
7. 启动仲裁状态
8. 跳转状态
9. 恢复模式状态

### 31.2 状态转换原则

- 任一状态失败都必须有明确去向
- 升级失败不应直接导致死机
- 没有合法升级盘时必须回到正常启动路径
- 没有合法 App 时必须进入恢复模式

---

## 32. 典型场景说明

### 32.1 场景一：正常上电，无 U 盘

流程如下：

1. 读取控制块。
2. 等待 U 盘窗口超时。
3. 无 U 盘。
4. 无待验证槽。
5. 当前活动槽合法。
6. 跳转当前活动槽。

### 32.2 场景二：插入普通 U 盘

流程如下：

1. 识别到 USB 设备。
2. 成功挂载文件系统。
3. 未发现合法升级目录或 manifest 不匹配。
4. 记录日志。
5. 返回正常启动决策。

### 32.3 场景三：插入合法升级盘，升级成功

流程如下：

1. 识别升级盘。
2. 解析 manifest。
3. 写入非活动槽 App。
4. 更新配置区。
5. 更新外置 Flash。
6. 设置待验证槽。
7. 启动新槽。
8. 新 App 启动成功并确认。
9. 新槽成为已确认槽。

### 32.4 场景四：新 App 启动失败

流程如下：

1. 升级成功后进入待验证启动。
2. 新 App 多次未完成确认。
3. Boot 识别尝试次数超限。
4. 回滚到已确认槽。
5. 记录回滚日志。

### 32.5 场景五：两个 App 都损坏

流程如下：

1. 无合法升级盘。
2. App1 校验失败。
3. App2 校验失败。
4. 进入恢复模式。
5. 等待合法升级盘恢复。

---

## 33. 参数可配置项设计

建议将以下项目做成可配置项：

- USB 等待时间
- 待验证启动最大尝试次数
- 是否允许版本回退
- 是否启用签名校验
- 是否启用强制升级模式 GPIO
- 外置 Flash 允许操作范围
- 日志详细程度
- 恢复模式下轮询周期

---

## 34. 设计约束与边界

### 34.1 Boot 边界

Boot 不应承担以下职责：

- 业务逻辑处理
- 长时间复杂计算
- 非必要的通信协议栈
- 通用文件管理器能力

### 34.2 升级边界

Boot 仅执行 manifest 显式定义的动作，不应根据文件名自动推测复杂行为。

### 34.3 数据边界

Boot 仅允许写入事先声明并受控的区域：

- App1 分区
- App2 分区
- Config 区指定区域
- 外置 Flash 指定范围

---

## 35. 测试设计建议

### 35.1 功能测试

建议覆盖以下测试项：

- 正常无 U 盘启动
- 普通 U 盘插入启动
- 合法升级盘升级 App
- 合法升级盘升级配置
- 合法升级盘升级外置 Flash
- 外置 Flash 导出
- 升级后首次启动确认

### 35.2 异常测试

建议覆盖以下异常场景：

- U 盘中途拔出
- 升级中断电
- manifest 缺失
- manifest 格式错误
- 文件 CRC 错误
- 写入失败
- 待验证槽启动失败
- Config 区控制块损坏
- App1 / App2 均损坏

### 35.3 边界测试

建议覆盖：

- 最小合法镜像
- 最大合法镜像
- 接近分区边界镜像
- 启动尝试次数边界
- 多次回滚
- 多次重复升级

---

## 36. 推荐实施顺序

为降低实施风险，建议按以下顺序推进：

### 第一阶段：核心启动框架

- 建立 Boot 独立工程
- 完成 Flash 分区与链接规划
- 完成 Boot 控制块双备份
- 完成 App 合法性校验
- 完成基础启动跳转

### 第二阶段：双分区与回滚

- 完成活动槽、确认槽、待验证槽机制
- 完成启动尝试计数
- 完成自动回滚策略

### 第三阶段：U 盘升级

- 完成 USB Host 和文件系统接入
- 完成升级盘合法性识别
- 完成 manifest 解析
- 完成 App 写入流程

### 第四阶段：配置与外置 Flash

- 完成配置区升级
- 完成外置 Flash 写入与导出
- 完成配置保护与备份

### 第五阶段：日志与恢复

- 完成 U 盘日志系统
- 完成内部摘要日志
- 完成恢复模式
- 完成强制升级模式

### 第六阶段：安全增强

- 版本控制策略
- 签名校验接口
- 安全升级机制扩展

---

## 37. 最终推荐方案总结

基于 STM32F429 的 Boot 设计，推荐采用以下总体策略：

1. 使用固定 Flash 分区实现 Boot、App1、Config、App2 四区布局。
2. 在 Config 区中使用双备份 Boot 控制块保存启动状态与升级状态。
3. App 镜像统一使用镜像头格式，Boot 通过镜像头和 CRC 实现合法性判定。
4. 上电后启动 USB Host，在默认 600ms 窗口内检测 U 盘。
5. 若检测到合法升级盘，则解析 `/boot/manifest.json` 执行升级动作。
6. App 升级默认写入非当前已确认槽，避免覆盖已知可运行版本。
7. 新升级镜像先进入待验证状态，只有 App 自行确认成功后才成为已确认版本。
8. 若待验证镜像启动失败，则自动回滚到已确认槽。
9. 若两个槽均不可启动，则进入恢复模式并等待升级盘恢复。
10. 所有升级和启动关键过程均记录日志，并支持写入 U 盘与内部摘要区。

---

## 38. 结论

本设计方案围绕“可启动、可升级、可回滚、可恢复、可追踪”五个核心目标构建，适合 STM32F429 平台的现场升级与双分区容灾需求。

该方案的关键价值在于：

- 避免单镜像升级带来的高风险
- 降低升级失败导致设备不可恢复的概率
- 通过待验证与确认机制实现可靠回滚
- 通过 manifest 和日志机制提升可维护性
- 通过模块化与 CMake 工程组织提升后续迭代效率

在不提供代码的前提下，本文档已给出完整设计边界、模块职责、状态流转和实施建议，可直接作为详细设计评审版基础文档使用。

---

## 39. 附录：术语说明

| 术语 | 说明 |
|------|------|
| 活动槽 | Boot 默认优先启动的 App 分区 |
| 已确认槽 | 已被证明可稳定运行的 App 分区 |
| 待验证槽 | 新升级后等待运行确认的 App 分区 |
| 回滚 | 待验证镜像失败后恢复到已确认镜像 |
| 升级盘 | 满足目录结构与匹配规则的专用 U 盘 |
| Manifest | 描述升级动作与校验信息的配置文件 |
| Boot 控制块 | 保存启动状态与升级状态的持久化数据结构 |

---

## 40. 附录：建议后续补充文档

建议在本详细设计文档基础上继续补充以下文档：

1. Boot 状态机说明文档
2. Manifest 格式规范文档
3. Boot 控制块数据格式文档
4. 错误码定义文档
5. Flash 分区与链接脚本说明文档
6. 升级包制作规范文档
7. 测试用例文档
